#include "main.h"

/**
 *_abs - The absolute value
 *@n: is the parameter that will be given an absolute value
 *Description: this  function takes the parameter and change it value
 *Return: -n or n
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
